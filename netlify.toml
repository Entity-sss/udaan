[build]
  base = ""
  command = "cd artifacts/udaan-ai && pnpm install --no-frozen-lockfile && pnpm run build"
  publish = "artifacts/udaan-ai/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_FLAGS = "--no-frozen-lockfile"